{% extends 'base-front.html.twig' %}

{% block title %}Edit Programme{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-info">
                    <div class="card-header bg-info text-white">
                        <h4 class="mb-0">Edit Programme</h4>
                    </div>
                    <div class="card-body">
                        {{ include('programme/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>
                <div class="mt-3">
                    <a href="{{ path('app_programme_index') }}" class="btn btn-outline-info">Back to List</a>
                </div>
                {{ include('programme/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
