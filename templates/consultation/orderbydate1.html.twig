{% extends 'base-front.html.twig' %}

{% block body %}
<style>
    table {
        width: 100%;
        border-collapse: collapse;
    }

    th, td {
        padding: 8px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    th {
        background-color: #f2f2f2;
    }

    tr:hover {
        background-color: #f5f5f5;
    }

    .action-links a {
        margin-right: 5px;
    }

    form label {
        display: inline-block;
        width: 150px; 
        text-align: center;
    }

    .submit-button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #005F6B; 
        color: white;
        text-align: center;
        text-decoration: none;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }

    .submit-button:hover {
        background-color: #005F6B; 
    }

    .consultation-cards {
        display: flex;
        flex-wrap: wrap;
        gap: 20px; 
    }

    .consultation-card {
        background-color: #f5f5f5; 
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
        width: calc(33.33% - 20px);
        color: black; 
    }

    .card-header {
        background-color: #0f4c75; 
        color: white; 
        font-size: 18px; 
        padding: 10px; 
        border-radius: 8px 8px 0 0; 
        width: 100%; 
        text-align: center; 
        margin-bottom: 10px; 
    }

    .card-details {
        margin-bottom: 10px;
    }

    .detail-label {
        font-weight: bold;
    }

    .action-links a {
        margin-right: 10px;
        text-decoration: none;
        color: black; 
    }

    .action-links a:hover {
        text-decoration: underline;
    }

    .consultation-card .consultation-id {
        font-size: 24px;
        font-weight: bold;
        color: white;
        background-color: #0f4c75;
        padding: 10px;
        border-radius: 8px;
        text-align: center;
    }

    .consultation-card .consultation-details {
        font-size: 16px;
    }
    .error-message {
    color: red;
}
.consultation-card:hover {
    background-color: #e0e0e0;
    transition: background-color 0.3s ease;
}

.action-links a {
    display: inline-block;
    padding: 8px 16px;
    background-color: #0f4c75;
    color: white;
    border-radius: 4px;
    text-decoration: none;
    transition: background-color 0.3s ease;
}

.action-links a:hover {
    background-color: #1b6ca8;
}
.warning-box {
    background-color: #ffcccc;
    color: red;
    padding: 5px 10px;
    border-radius: 4px;
    font-weight: bold;
    margin-top: 5px;
}


</style>
<form action="{{ path('search_consultation_by_date1') }}" method="get">
    <label for="start_date">Start Date:</label>
    <input type="date" id="start_date" name="start_date">
    <label for="end_date">End Date:</label>
    <input type="date" id="end_date" name="end_date">
    <button type="submit">Search</button>
</form>


<form action="{{ path('search_consultation1') }}" method="get">
    <label for="id">Search by ID:</label>
    <input type="text" id="id" name="id">
    <button type="submit">Search</button>
</form>

<div class="consultation-cards">
    {% for consultation in consultations %}
        {% if consultation.getIdt() == 5 %}
            <div class="consultation-card">
                <div class="card-details">
                    <div class="card-detail">
                        <span class="detail-label">Id consultation:</span>
                        <span class="detail-value">{{ consultation.getid() }}</span>
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Id fiche:</span>
                        {% if consultation.getFichemedicale() is null %}
                            {% set _ = consultation.setFichemedicale(fiche) %}
                            <span class="detail-value">pas de fiche médicale</span>
                        {% else %}
                            <span class="detail-value">{{ consultation.getFichemedicale().id }}</span>
                        {% endif %}
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Id Patient:</span>
                        <span class="detail-value">{{ consultation.getIdp() }}</span>
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Id Thérapeute:</span>
                        <span class="detail-value">{{ consultation.getIdt() }}</span>
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Date consultation:</span>
                        <span class="detail-value">{{ consultation.getDateC() ? consultation.getDateC()|date('Y-m-d') : '' }}</span>
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Pathologie:</span>
                        <span class="detail-value">{{ consultation.getpathologie }}</span>
                    </div>
                    <div class="card-detail">
                        <span class="detail-label">Remarques:</span>
                        <span class="detail-value">{{ consultation.getremarques }}</span>
                    </div>
                    <div class="card-details">
                        <!-- Other card details -->
                        <div class="card-detail">
                            <span class="detail-label">Confirmation:</span>
                            <span class="detail-value">{{ consultation.getConfirmation() ? 'True' : 'False' }}</span>
                        </div>
                    </div>
                </div>
                {% if consultation.getFichemedicale() is not null and consultation.getConfirmation() == false %}
                    <div class="warning-box">Non confimé</div>
                {% endif %}
                {% if consultation.getFichemedicale() is not null and consultation.getFichemedicale().id == 0 %}
                    <div class="warning-box">Fiche Non Attribuée</div>
                {% endif %}
                <div class="action-links">
                    <a href="{{ path('edit_consultation1', {'id': consultation.getid()}) }}">Edit</a>
                </div>
            </div>
        {% endif %}
    {% endfor %}
</div>


{% endblock %}
